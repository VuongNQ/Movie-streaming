{
  "name": "movie-streaming-crawler",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "crawl": "node src/index.js",
    "check": "node --check src/index.js && node --check src/adapters.js && node --check src/firestore.js",
    "playwright": "playwright",
    "playwright:install": "playwright install chromium",
    "playwright:codegen": "playwright codegen",
    "playwright:open": "playwright open"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "playwright": "^1.54.2"
  }
}